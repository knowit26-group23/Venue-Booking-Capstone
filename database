CREATE DATABASE IF NOT EXISTS VisitMyVenue_db;
USE VisitMyVenue_db;

CREATE TABLE users (
  userid INT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(100) NOT NULL,
  username VARCHAR(50) NOT NULL UNIQUE,
  email VARCHAR(120) NOT NULL,
  mobile VARCHAR(20) NOT NULL,
  password VARCHAR(100) NOT NULL,
  roleid INT NOT NULL
);
INSERT INTO users (name, username, email, mobile, password, roleid) VALUES
('System Admin', 'admin1', 'admin@bookmyvenue.com', '9000000001', 'admin123', 1),
('Asha Customer', 'customer1', 'asha@example.com', '9000000002', 'cust123', 2),
('Ravi Owner', 'owner1', 'ravi@example.com', '9000000003', 'owner123', 3),
('Meera Decor', 'provider1', 'meera@example.com', '9000000004', 'provider123', 4);

select * from users;
